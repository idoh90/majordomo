node_modules
dist
*.local
.DS_Store

.vercel
.env*

# agent scratch: throwaway worktrees and design-hook cache, not project state
.claude/worktrees
.impeccable

# ad-shots.mjs output — regenerate on demand, don't bloat the repo with PNGs
ads/screenshots/*.png
